{% extends 'layout' %}


{% block main %}

    <a href="javascript:history.back()">Zurück</a>


    <div class="content">
        <h1 class="title is-1">{{ entry.title }}</h1>
        <p>{{ entry.text}}</p>
    </div>

    <div class="tags">

        {% for tag in entry.tags.all() %}
            <span class="tag">
                {{ tag }}
            </span>
        {% endfor %}

    </div>
{% endblock %}
